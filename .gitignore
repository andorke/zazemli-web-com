# Reference docs (copied from ~/.claude/templates/project-docs/)
# Same for every project — not worth committing. Remove this entry if your team wants to version it.
example_docs/

# macOS
.DS_Store

# временные артефакты (figma-рендеры для сверки и пр.)
/tmp/

# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage
/test-results
/playwright-report

# next.js
/.next/
/out/
*.tsbuildinfo
next-env.d.ts

# debug
npm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# night-shift runtime (queue, state, logs) — не коммитится
.night/
