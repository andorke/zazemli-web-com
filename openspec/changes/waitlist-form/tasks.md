# waitlist-form — tasks

## 1. Контент и данные

- [ ] 1.1 Перенести тексты формы и согласий verbatim из `#waitModal` (`landing.html`) в контент-модуль; контент-тест на запрещённые формулировки («рассылк», «новост», «отписаться») и наличие «Это единственное письмо…»
- [ ] 1.2 Утилита обрезки ввода до 42 знаков; типы контракта API (`plant`, `requestId`, `consent_version: '2026-07-14'`)

## 2. Шаг 1 — плитка N°08

- [ ] 2.1 Форма шага 1 в плитке галереи: поле названия + «Дальше →»; ячейка грида не растягивается
- [ ] 2.2 Стили: служебные ссылки/тексты внутри плитки не получают карточных стилей (NEW-01)
- [ ] 2.3 Цель `waitlist_plant_input` со свойством `plant` через `lib/metrika`

## 3. Шаг 2 — попап

- [ ] 3.1 Нативный `<dialog>` + `showModal()` по эталону `#waitModal`: заголовок, имя растения как введено (именительный падеж), email, два чекбокса (иерархия: письмо 15px первым, ПДн ≥13px вторым, оба обязательны, непредзаполнены, таргеты 44px), «Записать меня →»
- [ ] 3.2 A11y: Esc, клик по фону, фокус-ловушка, возврат фокуса на плитку, крестик 44×44
- [ ] 3.3 Пять состояний: пусто / кривой email / без согласий / одно согласие / оба; фокус при ошибке — на первый неотмеченный
- [ ] 3.4 Цели `waitlist_form_open` / `waitlist_submit` / `waitlist_error`; других событий нет

## 4. Интеграция за флагом

- [ ] 4.1 Интеграционная точка по образцу diary-signup: POST шага 1 и шага 2 по контракту, за фича-флагом (по умолчанию выключен)
- [ ] 4.2 Тест: при выключенном флаге сетевых запросов нет; при включённом — два POST с полями контракта

## 5. Приёмка

- [ ] 5.1 Unit + e2e: шаг 1 → попап, клавиатурный контур, пять состояний (PATCH-1 §7.3)
- [ ] 5.2 `npm run typecheck && npm run test && npm run build` — зелёные
- [ ] 5.3 Записать в `CONTEXT.md`: юр-конструкция «не рассылка», операционное ограничение Sendsay (одно письмо, без промо), флаг юристу (38-ФЗ), зависимость включения от `waitlist-api`
