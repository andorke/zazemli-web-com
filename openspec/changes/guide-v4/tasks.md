# guide-v4 — tasks

## 1. Контент

- [x] 1.1 `sync-vault`; перенести `guide.md` v4.0 в `src/content/guide.ts`: вход (00, 01, развилка), перевалка (02–04), полная замена (01 Продолжение, 02–04); новые названия стадий
- [x] 1.2 Общие шаги «Дренаж», «Дневник», подсказка «Сохрани остаток грунта» — разделяемые объекты контента (определены один раз)
- [x] 1.3 Внешняя ссылка `epicgardening.com/root-rot/` с `target="_blank" rel="noopener"` (+комментарий «временная, заменить своей статьёй»)
- [x] 1.4 Обновить drift-тесты контента гайда против прототипов

## 2. Роуты и мета

- [x] 2.1 Создать `src/app/guide/perevalka/page.tsx` и `src/app/guide/polnaya-zamena/page.tsx`; перестроить вход `/guide` (стадии 00–01 + развилка, без шагов 02–04)
- [x] 2.2 `generateMetadata`: ветки — `noindex, follow` + canonical `https://zazemli.com/guide`; вход — canonical на себя, без noindex
- [x] 2.3 Ссылки: развилка → маршруты, «Это не мой случай» → соседняя ветка; относительных путей прототипов нет
- [x] 2.4 Sitemap не расширять (ветки noindex); проверить наличие HTML веток в `out/` после build

## 3. Компоненты и разметка

- [x] 3.1 Общие компоненты шагов «Дренаж»/«Дневник»/подсказки; ветки собираются из них
- [x] 3.2 Убрать блоки «Готово, когда» (FIX-62 отменён) и лид-строки под заголовками шагов; `details` работают без JS; анимация — только существующий `.reveal`
- [x] 3.3 HowTo JSON-LD: по одному на ветку из данных `guide.ts`; на входе HowTo убрать

## 4. Приёмка (PATCH-1 §7.2)

- [x] 4.1 Три URL открываются, ссылки в обе стороны работают; развилка после осмотра кома
- [x] 4.2 Мета веток/входа по контракту; общие шаги — не копипаста (посимвольная сверка веток)
- [ ] 4.3 Обновить e2e гайда; `npm run typecheck && npm run test && npm run build` — зелёные
- [ ] 4.4 Записать в `CONTEXT.md`: guide-канон = v4.0, выбор по HowTo (по ветке, вход без), FIX-62 отменён
