# legal-fixes — tasks

## 1. Адрес ИП (P0)

- [x] 1.1 Убрать адрес из `src/content/privacy.ts` (поле `address` и его рендер)
- [x] 1.2 Добавить регресс-тест: маркеры адреса (`Ковров`, `Строителей`) отсутствуют в `src/` и в собранном `out/`

## 2. Редакция /privacy

- [x] 2.1 Свежий `sync-vault` и сверка `src/content/privacy.ts` с актуальным прототипом `privacy.html` (13 разделов, ред. 14.07) — перенести дословно
- [x] 2.2 Обновить контент-тесты privacy (число/порядок разделов, формулировка про «Лист ожидания» ≠ рассылка, если есть в редакции)

## 3. Страница /terms

- [x] 3.1 Создать `src/content/terms.ts` из прототипа `terms.html` (8 разделов, врезка реквизитов без адреса, дата вступления в силу)
- [ ] 3.2 Создать роут `src/app/terms/` (typography-first вне DS, по образцу `/privacy`; canonical, индексируемая)
- [ ] 3.3 Добавить `/terms` в `sitemap.ts` (12 URL) и e2e-спек страницы
- [ ] 3.4 Ссылка «Условия использования сайта» → `/terms` в legal-блоке футера

## 4. Футер

- [ ] 4.1 Привести legal-строку к verbatim: «ИП Минетто А. А. · ОГРНИП 326330000022761 · работаем по УСН»; убедиться, что ИНН/адреса в футере нет
- [ ] 4.2 Соцупоминания IG/TG: реальный URL или текст без `<a>`; проверить отсутствие `href="#"` по всем страницам (grep по `out/` — включая якорные ссылки, которые обычный grep по `href="#"` не ловит)

## 5. Consent-значения

- [ ] 5.1 `src/lib/consent.ts`: тип `'necessary' | 'all'`, чтение легаси `granted→all` / `denied→necessary`, запись только новых значений
- [ ] 5.2 Кнопки и текст баннера: «Только необходимые» / «Принять все», нотис verbatim из PATCH-1 §4б со ссылкой на `/privacy`
- [ ] 5.3 Обновить потребителей (`Metrika`, баннер) и тесты (`cookie-banner.test.tsx`, consent-тесты); баннер присутствует на `/privacy` и `/terms`

## 6. Приёмка

- [ ] 6.1 `npm run typecheck && npm run test && npm run build` — зелёные; в `out/` есть `/terms`
- [ ] 6.2 Ручной прогон по PATCH-1 §7.3: cookie появление → выбор → не возвращается; до «Принять все» запросов к `mc.yandex.ru` нет
- [ ] 6.3 Записать в `CONTEXT.md`: источник утечки адреса (старый канон §4a) и механизм защиты; статус sign-off юриста не меняется
