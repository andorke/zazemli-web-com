# diary-signup-form — tasks

## 1. Контент

- [x] 1.1 `src/content/diary.ts`: типы + hero, gift, 3 выгоды, таймлайн 7 писем, эхо; тексты чекбоксов дословно из `diary-signup.md` v2.2; микрокопи состояний дословно из `error-message.md` v1.0.1 (валидация/network/5xx/maintenance/duplicate/confirmation); контент-тесты (7 писем, verbatim чекбоксов, микрокопи) — красные

## 2. Форма и страница

- [x] 2.1 Роут `app/diary-signup/page.tsx`: заглушка → страница-форма по прототипу, `noindex` сохранён, вне навигации/sitemap, один h1
- [x] 2.2 Клиентская форма: валидация email (формат, `aria-invalid`, ok/err), два непредзаполненных чекбокса, блокировка submit до обоих согласий, фокус на первую ошибку
- [x] 2.3 Состояния и confirmation: подмена формы confirmation-блоком при успехе, микрокопи ошибок из `diary.ts`
- [ ] 2.4 Попап-резюме политики «Коротко о данных» (модалка: Esc/фон/крестик) со ссылкой «Читать полную политику →» на `/privacy`; ссылка в CB1 → `/privacy`
- [ ] 2.5 Секции «Почему тебе» (3 выгоды) и «Что внутри» (таймлайн 7 писем), эхо + повтор CTA

## 3. Аналитика и интеграция

- [ ] 3.1 Event `diary_signup_submit` через `lib/metrika` за consent-gate на успешный submit
- [ ] 3.2 Интеграционная точка submit: обёртка POST на РФ-эндпоинт, по умолчанию выключена (флаг/`null`); при выключенной — реального POST с ПДн нет, форма валидируется и показывает состояния; отправляемая полезная нагрузка включает версию формулировки согласия (для лога ≥3 года на бэкенде)

## 4. Верификация

- [ ] 4.1 e2e: submit заблокирован без обоих согласий; невалидный email → ошибка + focus; happy-path → confirmation + event; ссылка CB1 и попап ведут на `/privacy`; `noindex` сохранён, нет в навигации/sitemap
- [ ] 4.2 Полный прогон unit + e2e + static export + ds-lint; визуальная сверка с прототипом на 1440px и 360px (форма above the fold на мобиле), `prefers-reduced-motion`
- [ ] 4.3 Обновить CONTEXT.md; в вопросы Насте: дата включения реального сбора (РФ-хостинг + Sendsay + уведомление РКН), поведение формы при выключенном сборе
